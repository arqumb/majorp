╔══════════════════════════════════════════════════════════════════════════════╗
║                    AutoScraper AI - Setup Cheat Sheet                        ║
║                     Print This for Easy Reference!                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: DOWNLOAD PROJECT                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

  1. Go to: https://github.com/arqumb/majorp
  2. Click green "Code" button → Download ZIP
  3. Extract ZIP to Desktop or Documents folder
  4. Open folder "majorp"

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: AUTOMATIC INSTALLATION (EASIEST!)                                    │
└──────────────────────────────────────────────────────────────────────────────┘

  Windows:
    → Double-click "QUICK_START.bat"
    → Wait 5-15 minutes
    → Done!

  Mac/Linux:
    → Open Terminal in project folder
    → Type: chmod +x QUICK_START.sh
    → Type: ./QUICK_START.sh
    → Wait 5-15 minutes
    → Done!

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: TEST INSTALLATION                                                    │
└──────────────────────────────────────────────────────────────────────────────┘

  1. Open Command Prompt (Windows) or Terminal (Mac/Linux)
  2. Navigate to project:
     cd Desktop/majorp

  3. Activate environment:
     Windows: venv\Scripts\activate
     Mac/Linux: source venv/bin/activate

  4. Run test:
     python main.py "https://quotes.toscrape.com" "extract quotes" --output test.json

  5. Check if "test.json" file was created ✓

┌──────────────────────────────────────────────────────────────────────────────┐
│ DAILY USAGE (EVERY TIME YOU WANT TO SCRAPE)                                  │
└──────────────────────────────────────────────────────────────────────────────┘

  1. Open Command Prompt/Terminal
  
  2. Go to project:
     cd Desktop/majorp
  
  3. Activate environment:
     Windows: venv\Scripts\activate
     Mac/Linux: source venv/bin/activate
  
  4. Run scraper:
     python main.py "YOUR_URL" "YOUR_TASK" --output results.json
  
  5. Open results.json to see data!

┌──────────────────────────────────────────────────────────────────────────────┐
│ EXAMPLE COMMANDS (COPY & PASTE THESE!)                                       │
└──────────────────────────────────────────────────────────────────────────────┘

  Extract Quotes:
  python main.py "https://quotes.toscrape.com" "extract quotes and authors" --output quotes.json

  Extract Products:
  python main.py "https://shop.com/products" "extract product names and prices" --output products.json

  Extract News:
  python main.py "https://news-site.com" "extract article titles" --output news.json

┌──────────────────────────────────────────────────────────────────────────────┐
│ COMMAND FORMAT                                                                │
└──────────────────────────────────────────────────────────────────────────────┘

  python main.py "URL" "TASK" --output FILENAME.json

  Example:
  python main.py "https://example.com" "extract titles" --output my_data.json
                 └─────────┬─────────┘ └──────┬──────┘          └────┬────┘
                      Website URL        What to extract      Save location

┌──────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                               │
└──────────────────────────────────────────────────────────────────────────────┘

  Problem: "python: command not found"
  Fix: Try "python3" instead of "python"

  Problem: "pip: command not found"
  Fix: python -m pip install --upgrade pip

  Problem: "ModuleNotFoundError"
  Fix: Make sure virtual environment is activated!
       Windows: venv\Scripts\activate
       Mac/Linux: source venv/bin/activate

  Problem: Installation is slow
  Fix: This is normal! First time downloads ~300MB of browsers

  Problem: "404 error" during scraping
  Fix: This is normal! System automatically tries dynamic loading

┌──────────────────────────────────────────────────────────────────────────────┐
│ REQUIREMENTS                                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

  ✓ Python 3.8 or higher
  ✓ 2GB free disk space
  ✓ Internet connection
  ✓ Windows 10+, macOS 10.13+, or Linux

┌──────────────────────────────────────────────────────────────────────────────┐
│ IMPORTANT FILES                                                               │
└──────────────────────────────────────────────────────────────────────────────┘

  main.py                  → Run this to scrape
  SETUP.md                 → Detailed setup instructions
  QUICK_REFERENCE.md       → Quick command reference
  INSTALL_INSTRUCTIONS.txt → Installation help
  requirements.txt         → List of dependencies

┌──────────────────────────────────────────────────────────────────────────────┐
│ GETTING HELP                                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

  1. Read SETUP.md for detailed instructions
  2. Check GitHub: https://github.com/arqumb/majorp/issues
  3. Create new issue with error details

┌──────────────────────────────────────────────────────────────────────────────┐
│ TIPS FOR SUCCESS                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  ✓ Always use quotes around URL and task
  ✓ Be specific in task description
  ✓ First run is slower (downloads browsers)
  ✓ Second run is faster (uses cached rules)
  ✓ Check output JSON file for results
  ✓ Use --force-new to ignore cached rules

╔══════════════════════════════════════════════════════════════════════════════╗
║                              Happy Scraping! 🚀                              ║
╚══════════════════════════════════════════════════════════════════════════════╝